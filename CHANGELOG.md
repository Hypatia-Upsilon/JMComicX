# 更新日志

## 未发布

### 新功能

- 漫画更新提示：书架与收藏里的漫画有更新时给出提示
  - 「我的」页「漫画收藏」行右侧显示红底数字，表示收藏里有几部漫画更新；只统计收藏成员，仅在书架、未被收藏的漫画不计入
  - 進入漫画收藏后，有更新的漫画封面右上角显示「更新N章」；N 为上次看到该漫画时的章节结尾与当前章节结尾之差
  - 书架页中，有更新的漫画所在分组标签右上角显示红点；同一部漫画同时属于「全部」与自建分组时，点开该漫画（无需阅读）后所有相关标签的红点一并消失
  - 书架页新增下拉刷新：除重新读取本地书架外，还会强制检查一次更新，用于兜住"正好在下拉时某部漫画更新"的极端情况
  - 更新检查在后台进行，冷启动与回到前台各探一次，两次之间设有最小间隔，避免频繁请求
  - 设置页新增「漫画更新提示」区块：可立即检查、清除全部提示，并提供「模拟一次漫画更新」用于在没有真实更新时验证整条链路
- 书架页与收藏页的排序新增「正序 / 倒序」
  - 展开「排序方式」后，名称 / 更新时间 / 最近阅读在二级菜单里；同一菜单底部还有一个「倒序」勾选项，勾选即倒序、不勾即正序
  - 名称：正序 A→Z，倒序 Z→A
  - 更新时间 / 最近阅读：正序为最新在前，倒序为最久在前
  - 方向按字段分别记住，切换排序字段不会莫名翻转方向
  - 收藏页顶栏排序菜单同样采用这一交互；因收藏接口只提供最新在前的顺序，倒序通过翻转分页实现
- 搜索的标签过滤改版（#14）
  - 搜索栏在输入状态下也显示标签过滤入口，不再只在结果页出现
  - 标签按钮在未启用过滤时为默认外观，启用后才高亮，不再始终带一层蓝色遮罩
  - 过滤支持包含与排除两组标签

### 优化

- 搜索结果过滤下推服务端，减少本地过滤带来的结果展示延迟

### 修复

- 修复书架分组超过 10 个时，漫画详情页的「加入书架」浮窗无法滚动、取消与确认按钮被挤出屏幕的问题
- 修复时间类排序「正序 / 倒序」语义颠倒的问题（正序应为最新在前）

## 2.6.3 - 2026-08-24

### 新功能

- 书架新增「分组序列」页：右上角展开栏进入，一行一个分组，从上到下就是书架顶部 tab 栏从左到右的顺序
  - 每行显示序号、分组名与漫画数；单击一行返回书架并定位到该分组，此时若有拖好还没保存的顺序会一并保存
  - 按住行右侧的手柄（三条横线）上下拖动换位置，其余行实时让位，拖到上下边缘会自动滚动；「全部」锁定在第一位
  - 顶栏下方有搜索框：输入分组名后回车、或点一下搜索图标，列表定位至那个分组
  - 拖动过顺序后右上角才出现「完成」，按下保存并返回；返回时若有未保存的调整会先问一句
  - 右上角下拉菜单新增「自动排列」（默认关闭）：按分组使用热度把常看的排在前面，开启期间手柄不响应拖动；手动顺序始终保留，关掉即原样恢复
  - 分组顺序与热度都只存本机，不进书架导出文件
- 阅读器新增「持续阅读」（默认开启，可在阅读设置里关闭）：在章末继续上拉、或在章首继续下拉，出现提示卡，越过阈值震动一次，松手换章
  - 提示卡分两段：先是「继续上拉进入下一话」并给出下一话的话名与页数，越过阈值后箭头翻面、改成「松手进入」
  - 越过阈值后不松手反向拖回来，进度条同步退回；退干净就取消这一次换章
  - 向下进入下一话落在第一页；向上进入上一话落在最后一页
  - 最后一话下拉、第一话上拉只出提示，不震动也不跳转；双指放大状态下手势不触发

### 优化

- 书架分组不再有 40 个上限（#10）
- 阅读器加载算法重做
  - 页面占位高度由写死的 720dp 改为按实测高宽比估算
  - 预取由「当前页下完之后、串行往后取 2 页」改为窗口式
  - 混淆图改用自带解码器一步还原

### 修复

- 修复阅读器每页出现等距横线的问题
- 修复阅读器加载图片时的闪烁抖动
- 修复翻到最底再往上翻时，上方图片加载完会把视图重新送回底部的问题
- 修复分组数达到上限后新建分组失败却提示「名称重复」的误导性提示（#10）