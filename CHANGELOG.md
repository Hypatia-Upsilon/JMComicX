# 更新日志

## 2.5.0 - 2026-08-21

### 新功能

- 书架新增导出与导入：右上角导出、左上角导入。
  - 导出为固定格式的 JSON 文件并写入系统“Download”目录
  - 导入文件时可选择“合并到当前书架”或“替换当前书架”；替换为不可撤销操作
  - 合并按分组**名称**归并，不会出现两个同名分组；已在书架里的漫画只补分组归属，本地阅读进度一律不覆盖
- 收藏页新增排序方式切换（更新时间 / 收藏时间）
- 书架分组管理添加"按作者自动收录"方式 可选检索范围（我的收藏 / 所有作品）（#8）
- 设置新增“内容语言”（简体中文 / 繁體中文），标题、简介与标签跟随切换，缓存按语言分开存放
- 设置新增“悬浮底栏”开关与样式选择（MIUIX / iOS-like），并新增“顶栏模糊样式”选项。
- 设置新增“顶栏模糊样式”选项 （高斯模糊 /渐变模糊 ）

### 优化
  
- 图片加载升级到 Coil 3
- 完善部分历史及新增功能涉及代码注释 **by Claude**
- 图片磁盘缓存上限由 96MB 提升到 256MB，内存缓存占比提升到可用堆的 25%；内存回收改为按系统压力分级，切到后台再切回不再重新解码所有封面。
- 线路选择改为按健康度（成功率与时延）评分：首次请求走当前最健康的线路，服务端错误或网络错误换机（#7）
- 漫画图片下载失败时会在同一线路换后缀重试、必要时换线路重下并丢弃半截数据（#4）
- 并发的相同请求合并为一次网络往返（#4）

### 修复

- 修复账号凭据未真正持久化的问题
- 修复整个界面在加载即将完成时卡顿的问题（#4）
- 修复阅读页拖动进度条跳转失效
- 修复签到接口返回空 payload 时被判为解析失败的问题

### 其他

- 剔除未接入任何入口的“启动选线门控页”，以及一批无引用的诊断、加密、下载与图片辅助方法
- 移除与应用内实现重复的 `samples/` 示例、失效的依赖版本声明与本地构建临时约束
- 补齐书架导入导出的单元测试，并修正一处仍在断言旧图片缓存上限的过期测试

### 关联 Issue

- 已解决：#4 加载优化、#8 书架优化
- 部分解决：#7 网络切换 —— 线路健康度评分、失败换线与设置页测速/手动选线已实现，「进入 App 前的选线页」未做
- 未解决：#5 浏览下一章优化 —— 需要改造阅读器状态模型，留到后续版本